# Copy to `.env` and fill in your local PostgreSQL password from the PGAdmin install wizard.

# Database name for this project (create with db/sql/01_create_database.sql in PGAdmin).
# Attach the connection under your PGAdmin server group "Guilds of Ardessia".
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/guilds_of_ardessia
